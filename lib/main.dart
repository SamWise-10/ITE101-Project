import 'package:flutter/material.dart';
import 'tdlf_educ_app.dart';

// Standalone entry point. When embedded in the Tawi-Tawi super-app, the host
// opens `const TdlfEducApp()` directly instead of going through this main().
void main() {
  runApp(const TdlfEducApp());
}
